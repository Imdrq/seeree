# TASK — SiriAI for Windows

> 开发任务分解 | 版本 v1.0 | 2026-07-21

---

## 阶段总览

| 阶段 | 名称 | 预计工期 | 任务数 | 状态 |
|------|------|----------|--------|------|
| P1 | Electron 骨架 + 透明悬浮窗 | 2-3 天 | 8 | ⬜ 待开始 |
| P2 | 液态玻璃气泡渲染 | 4-6 天 | 10 | ⬜ 待开始 |
| P3 | Dynamic Island 展开动效 | 3-4 天 | 7 | ⬜ 待开始 |
| P4 | Siri 波形动画 + 语音 | 3-4 天 | 8 | ⬜ 待开始 |
| P5 | ChatGPT 集成 + 对话面板 | 3-4 天 | 9 | ⬜ 待开始 |
| P6 | 设置 + 打包 + 发布 | 3-5 天 | 8 | ⬜ 待开始 |

---

## P1：Electron 骨架 + 透明悬浮窗（2-3 天）

> 目标：得到一个可以跑的无边框透明窗口，悬浮在桌面右上角，带系统托盘图标和全局快捷键。

---

### T1.1 — 项目脚手架初始化

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | — |
| **描述** | 使用 electron-vite 初始化项目，配置 TypeScript + React + Tailwind CSS |

**子任务：**

- [ ] `pnpm create @electron-vite/create` 初始化项目模板
- [ ] 配置 `tsconfig.json`（主进程 + 渲染进程双配置）
- [ ] 安装并配置 Tailwind CSS (`tailwind.config.ts`, `postcss.config.js`)
- [ ] 配置 ESLint + Prettier
- [ ] 配置 `electron-builder.yml` 基础配置
- [ ] 验证：`pnpm dev` 能正常启动 Electron 窗口

**产出文件：**
```
package.json, tsconfig.json, tsconfig.node.json, tsconfig.web.json
tailwind.config.ts, postcss.config.js
electron-builder.yml
.eslintrc.js, .prettierrc
```

---

### T1.2 — 透明无边框窗口

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T1.1 |
| **描述** | 配置 Electron BrowserWindow 为透明无边框模式 |

**子任务：**

- [ ] `electron/main.ts` 中创建窗口，设置 `transparent: true, frame: false, alwaysOnTop: true`
- [ ] 设置窗口初始大小 140×140（气泡态尺寸）
- [ ] 窗口定位到屏幕右上角（`screen.getPrimaryDisplay().workAreaSize` 计算）
- [ ] 移除背景色，让窗口完全透明
- [ ] 验证：窗口显示为完全透明的区域

**验收标准：**
- 窗口在屏幕上但不可见（完全透明）
- 任务栏不显示该窗口图标（`skipTaskbar: true`）
- 验证命令：在窗口中放一个红色 div 确认渲染正常

---

### T1.3 — 点击穿透机制

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 2h |
| **依赖** | T1.2 |
| **描述** | 透明区域点击穿透到桌面，仅气泡区域可点击 |

**子任务：**

- [ ] 实现 `setIgnoreMouseEvents(true, { forward: true })` 默认点击穿透
- [ ] 在气泡区域（中央圆形 div）上 `pointer-events: auto`
- [ ] 通过 `mouseenter` / `mouseleave` 事件动态切换穿透
- [ ] 验证：气泡区域可点击，透明区域点击穿透到桌面

**验收标准：**
- 气泡区域可以响应点击、hover
- 透明区域操作直接穿透到桌面应用
- 无闪烁或延迟

---

### T1.4 — 系统托盘

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T1.2 |
| **描述** | 系统托盘驻留图标，支持右键菜单 |

**子任务：**

- [ ] 设计/准备托盘图标（16×16、32×32 各一版）
- [ ] 创建 `electron/tray.ts`，初始化 `Tray`
- [ ] 右键菜单：显示/隐藏、设置、退出
- [ ] 实现 `show/hide/quit` 逻辑
- [ ] 托盘图标 tooltip 显示 "SiriAI"

**验收标准：**
- 托盘区域显示图标
- 右键菜单三项功能正常
- 退出后托盘图标消失

---

### T1.5 — 全局快捷键

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 1.5h |
| **依赖** | T1.3 |
| **描述** | 支持 `Alt+Space` 全局唤起/隐藏窗口 |

**子任务：**

- [ ] `electron/main.ts` 中使用 `globalShortcut.register('Alt+Space', ...)`
- [ ] 实现 toggle 逻辑：隐藏 ↔ 显示（Idle 态）
- [ ] 快捷键注册失败时输出警告日志
- [ ] 退出时 `globalShortcut.unregisterAll()`

**验收标准：**
- 按 `Alt+Space` 窗口从隐藏变为显示
- 再次按下窗口隐藏
- 与其他应用不冲突（仅在窗口关闭时注册）

---

### T1.6 — 失焦自动隐藏

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 1.5h |
| **依赖** | T1.3 |
| **描述** | 窗口失焦后延迟 3 秒自动隐藏 |

**子任务：**

- [ ] 监听 `BrowserWindow` 的 `blur` 事件
- [ ] 延迟 3 秒后调用 `win.hide()`
- [ ] 如果在 3 秒内重新 focus，取消隐藏
- [ ] 仅在 IDLE 状态生效（ACTIVE/EXPANDED 不自动隐藏）

**验收标准：**
- 点击桌面其他位置，3 秒后窗口自动隐藏
- 快速切回窗口，不会隐藏
- 正在对话时不会自动隐藏

---

### T1.7 — React App 骨架

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 2h |
| **依赖** | T1.1 |
| **描述** | 搭建 React 渲染进程的基础组件结构 |

**子任务：**

- [ ] `src/main.tsx` React 入口，挂载到 `#root`
- [ ] `src/App.tsx` 根组件，使用 Zustand 管理 `windowState`
- [ ] 创建 `src/store/appStore.ts`（Zustand），定义 `windowState` + 基础 actions
- [ ] 创建 `src/components/LiquidBubble/index.tsx` 占位组件（显示一个圆形 div）
- [ ] 配置全局 CSS（`src/styles/index.css`）引入 Tailwind 指令

**验收标准：**
- `pnpm dev` 启动后在透明窗口中看到一个圆形的占位气泡
- Zustand DevTools 可看到状态变化

---

### T1.8 — 窗口尺寸切换基础实现

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T1.2, T1.7 |
| **描述** | 实现 IPC 通信 + 窗口尺寸动态切换 |

**子任务：**

- [ ] `electron/preload.ts` 中使用 `contextBridge.exposeInMainWorld` 暴露 API
- [ ] 实现 `setWindowSize` IPC handler（主进程）
- [ ] `src/hooks/useWindowState.ts` hook，封装窗口状态切换 + IPC 调用
- [ ] 点击气泡 → 切换到 ACTIVE 状态 → 窗口变大到 300×90

**验收标准：**
- 点击气泡后窗口从 140×140 变为 300×90
- 过渡动画平滑（窗口 resize 动画）
- 类型安全：TypeScript 类型定义覆盖 `window.electronAPI`

---

## P2：液态玻璃气泡渲染（4-6 天）

> 目标：实现液态玻璃气泡的 3D 渲染，包括 GLSL 着色器、背景捕获、呼吸动画。这是整个项目的视觉名片。

---

### T2.1 — Three.js + R3F 基础场景

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | P1 完成 |
| **描述** | 搭建 Three.js 渲染环境，渲染一个基础球体 |

**子任务：**

- [ ] 安装 `three`, `@react-three/fiber`, `@react-three/drei`
- [ ] 创建 `src/components/LiquidBubble/BubbleCanvas.tsx`
- [ ] 配置正交相机，正对 XY 平面
- [ ] 渲染一个 `SphereGeometry`（128 段细分），基础 `MeshStandardMaterial`
- [ ] 添加环境光 + 点光源
- [ ] 验证：透明窗口中看到一个 3D 球体

**验收标准：**
- 3D 球体正确渲染在透明窗口中
- 球体大小约 100px 直径
- 帧率稳定 60 FPS

---

### T2.2 — GLSL 玻璃着色器（初版）

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 6h |
| **依赖** | T2.1 |
| **描述** | 编写 GLSL 着色器实现玻璃材质效果 |

**子任务：**

- [ ] 创建 `src/components/LiquidBubble/shaders/glass.vert` 顶点着色器
- [ ] 创建 `src/components/LiquidBubble/shaders/glass.frag` 片元着色器
- [ ] 实现菲涅尔反射效果（`fresnel = pow(1 - dot(N, V), 3)`）
- [ ] 实现边缘高光
- [ ] 实现基础的颜色渐变（中心透明 → 边缘半透明）
- [ ] 使用 `shaderMaterial` 替换 `MeshStandardMaterial`
- [ ] 调试着色器编译错误

**验收标准：**
- 球体呈现半透明玻璃感
- 边缘有明显的高光环
- 中心比边缘更透明
- 无着色器编译错误

---

### T2.3 — 桌面背景捕获与纹理映射

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | T2.2 |
| **描述** | 捕获桌面壁纸作为背景纹理，实现"透视"效果 |

**子任务：**

- [ ] 主进程实现 `capture-desktop` IPC handler（`desktopCapturer.getSources`）
- [ ] preload 暴露 `captureDesktop()` API
- [ ] 渲染进程定期调用捕获（每 500ms）
- [ ] Base64 → `THREE.CanvasTexture` 转换
- [ ] 将纹理作为 uniform `uBackground` 传入着色器
- [ ] 着色器中实现背景采样 + UV 偏移（模拟折射）
- [ ] 窗口不可见时停止捕获

**验收标准：**
- 气泡能"透视"看到桌面背景
- 背景有一定程度的扭曲/偏移（折射感）
- 性能：捕获不造成明显卡顿
- 窗口隐藏后不再执行捕获

---

### T2.4 — 模糊 + 色散效果

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 4h |
| **依赖** | T2.3 |
| **描述** | 在着色器中实现高斯模糊采样和 RGB 色散 |

**子任务：**

- [ ] 着色器中实现多次偏移采样的模糊近似（4-8 次采样）
- [ ] 实现 RGB 通道分离采样（chromatic aberration）
- [ ] 色散强度随法线方向变化（正视无色散，边缘较强）
- [ ] 调试模糊半径和色散强度的视觉参数
- [ ] 确保模糊采样总数不过多（< 12 次，避免性能问题）

**验收标准：**
- 气泡透视的桌面背景有朦胧模糊感
- 边缘能看到 RGB 分离的彩虹色边
- 在集成显卡上仍保持 55+ FPS

---

### T2.5 — 呼吸动画 + 悬停交互

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 3h |
| **依赖** | T2.4 |
| **描述** | 实现气泡的呼吸脉动和鼠标悬停效果 |

**子任务：**

- [ ] `useFrame` 中实现 `uTime` 和 `uBreath` uniform 更新
- [ ] 呼吸动画：`breath = sin(time * 0.8) * 0.5 + 0.5`，3-4 秒周期
- [ ] 呼吸影响：气泡 scale 微变（0.97 → 1.03）+ 亮度微变
- [ ] 鼠标 hover：气泡轻微放大（1.05x）+ 跟随鼠标方向微倾斜
- [ ] 鼠标位置通过 `useFrame` 传入 `uMouse` uniform
- [ ] 悬停时呼吸动画暂停，保持放大状态

**验收标准：**
- 气泡持续进行柔和的呼吸脉动
- 鼠标悬停时气泡响应式放大
- 鼠标移开恢复呼吸
- 动画自然不突兀

---

### T2.6 — Drei 辅助效果（可选增强）

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 2h |
| **依赖** | T2.5 |
| **描述** | 使用 @react-three/drei 增加后期处理和环境效果 |

**子任务：**

- [ ] 添加 `Environment` 预设环境贴图
- [ ] 添加 `Bloom` 后处理（边缘辉光 bloom）
- [ ] 添加 `Vignette` 暗角效果
- [ ] 平衡性能：后处理仅使用 1-2 个 pass

**验收标准：**
- 气泡边缘有柔和的辉光
- 视觉效果更接近 iOS Siri 质感
- 帧率仍保持 50+ FPS

---

### T2.7 — 动画库安装与基础配置

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 1h |
| **依赖** | T1.7 |
| **描述** | 安装 framer-motion，配置基础动画环境 |

**子任务：**

- [ ] 安装 `framer-motion` 依赖
- [ ] 在 `src/App.tsx` 中使用 `AnimatePresence` 包裹组件
- [ ] 创建动画配置常量（spring 参数）
- [ ] 配置 `LayoutGroup` 为后续 layoutId 动画做准备

**验收标准：**
- framer-motion 安装正常
- `AnimatePresence` 基础配置就绪
- 动画常量可被其他组件引用

---

### T2.8 — CSS 降级方案（兼容性）

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 3h |
| **依赖** | T2.1 |
| **描述** | 实现 WebGL 不可用时的 CSS 纯毛玻璃降级方案 |

**子任务：**

- [ ] WebGL 2.0 能力检测（`canvas.getContext('webgl2')`）
- [ ] 创建 `src/components/LiquidBubble/CSSBubble.tsx`（纯 CSS 版气泡）
- [ ] CSS `backdrop-filter: blur(30px)` + 渐变边框
- [ ] `border: 1px solid rgba(255,255,255,0.3)`
- [ ] CSS `@keyframes` 实现呼吸动画
- [ ] 在 `BubbleCanvas` 中根据 WebGL 支持自动切换

**验收标准：**
- WebGL 不可用时自动使用 CSS 方案
- CSS 版本也有基础的毛玻璃感
- 切换过程无闪烁

---

### T2.9 — 气泡渲染性能基准测试

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T2.5 |
| **描述** | 建立性能基准，确保渲染达标 |

**子任务：**

- [ ] 使用 Chrome DevTools Performance 录制 10 秒渲染
- [ ] 记录平均/最低 FPS
- [ ] 记录 GPU 内存占用
- [ ] 记录 CPU 占用率（任务管理器）
- [ ] 目标：平均 ≥ 55 FPS，CPU < 5%，GPU 内存 < 50MB

**验收标准：**
- 性能数据记录并归档
- 识别并记录任何性能瓶颈
- 如不达标，提出优化方案

---

### T2.10 — 着色器参数调优 UI（开发工具）

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 2h |
| **依赖** | T2.4 |
| **描述** | 临时开发面板，用于微调着色器参数 |

**子任务：**

- [ ] 使用 leva 或简单的 range input 控件
- [ ] 可调节参数：模糊半径、色散强度、菲涅尔强度、透明度
- [ ] 实时预览效果变化
- [ ] 导出确定的最佳参数值写入着色器
- [ ] 开发完成后移除（生产环境不包含）

**验收标准：**
- 能通过滑块实时调整着色器效果
- 确定一套最佳默认参数
- 面板可在构建时通过环境变量移除

---

## P3：Dynamic Island 展开动效（3-4 天）

> 目标：实现 bubble → capsule → panel 三级渐进的丝滑展开动画。

---

### T3.1 — layoutId 气泡↔胶囊切换

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | P2 核心渲染完成, T2.7 |
| **描述** | 使用 framer-motion layoutId 实现气泡到胶囊的形态过渡 |

**子任务：**

- [ ] 气泡组件和胶囊组件使用相同的 `layoutId="bubble"` 
- [ ] 气泡：`borderRadius: 50%`（正圆）
- [ ] 胶囊：`borderRadius: 40px`，`width: 280px, height: 80px`
- [ ] 测试 `AnimatePresence` 模式切换
- [ ] 调整 spring 参数使过渡丝滑（stiffness: 170, damping: 26）

**验收标准：**
- 点击气泡后平滑过渡为胶囊形状
- 无跳变、无闪烁
- 动画曲线接近 iOS 风格

---

### T3.2 — 胶囊内容区实现

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T3.1 |
| **描述** | Dynamic Island 胶囊内的状态文字和指示器 |

**子任务：**

- [ ] 创建 `src/components/DynamicIsland/IslandHeader.tsx`
- [ ] 状态文字映射：listening → "正在聆听…"，processing → "思考中…"，responding → "回答中…"
- [ ] 旋转指示器动画（framer-motion `rotate` 无限循环）
- [ ] 文字淡入动画
- [ ] 胶囊背景：玻璃态（`backdrop-filter: blur(20px)`）

**验收标准：**
- 胶囊内显示对应状态文字
- 思考中时有旋转小圆环
- 文字切换有淡入效果

---

### T3.3 — 胶囊 → 面板展开动画

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | T3.2 |
| **描述** | 点击胶囊后向下展开对话面板 |

**子任务：**

- [ ] 窗口尺寸从 300×90 过渡到 400×580（IPC `setWindowSize`）
- [ ] 面板容器从 `height: 0` → `height: auto`（framer-motion）
- [ ] 面板从胶囊底部滑出，顶部胶囊保持在原位
- [ ] 面板内容区淡入
- [ ] 面板内容采用虚拟列表（对话量大时优化）

**验收标准：**
- 点击胶囊后平滑展开为对话面板
- 面板顶部胶囊保持不变
- 面板内容从下方滑出

---

### T3.4 — 展开/收起完整状态机

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T3.1, T3.3 |
| **描述** | 实现 IDLE/ACTIVE/EXPANDED/HIDDEN 四种状态的完整切换逻辑 |

**子任务：**

- [ ] 在 Zustand 中实现完整的状态转换逻辑
- [ ] 状态转换触发源映射：
  - 点击气泡 → IDLE → ACTIVE
  - 点击胶囊 → ACTIVE → EXPANDED
  - 面板外点击 / ESC → EXPANDED → ACTIVE
  - 失焦超时 → ACTIVE → IDLE
  - 失焦超时 → IDLE → HIDDEN
- [ ] 每个转换触发对应的窗口 resize + 动画
- [ ] 编写状态机单元测试

**验收标准：**
- 四种状态之间切换流畅
- 不会出现非法状态转换
- 动画不重叠、不冲突

---

### T3.5 — 弹性动画曲线微调

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T3.4 |
| **描述** | 微调动画参数，使其更接近 iOS Siri 的弹性手感 |

**子任务：**

- [ ] 测试不同的 spring 参数组合
- [ ] 对比 iOS Siri 参考视频的动画节奏
- [ ] 调整 bounce/stiffness/damping 三参数
- [ ] 确保在 60fps 和 120fps 刷新率下表现一致

**验收标准：**
- 展开动画有明显的弹性"弹跳"感，但不过度
- 收起动画快速且干脆
- 视觉上与 iOS Siri 动效接近

---

### T3.6 — 面板外点击关闭

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 1.5h |
| **依赖** | T3.4 |
| **描述** | 点击面板外的透明区域关闭面板 |

**子任务：**

- [ ] 在窗口层级添加一个全尺寸的透明遮罩层
- [ ] 遮罩层 `onClick` 触发面板收起
- [ ] 遮罩层不影响面板内的点击事件（事件冒泡阻止）
- [ ] 确保点击穿透在遮罩层不激活时不生效

**验收标准：**
- 面板展开时，点击面板外区域面板收起
- 点击面板内不会误触发
- ESC 键也能收起面板

---

### T3.7 — 窗口拖拽定位

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 2h |
| **依赖** | T3.4 |
| **描述** | 支持用户拖拽窗口到任意位置并记住位置 |

**子任务：**

- [ ] 实现 `-webkit-app-region: drag` 拖拽区域（气泡/胶囊头部）
- [ ] IDLE 态气泡整体可拖拽
- [ ] ACTIVE/EXPANDED 态胶囊头部可拖拽
- [ ] 拖拽后位置保存到 `localStorage`
- [ ] 下次启动恢复上次位置

**验收标准：**
- 可以拖拽气泡到桌面任意位置
- 重启应用后位置保留
- 多显示器环境位置计算正确

---

## P4：Siri 波形动画 + 语音（3-4 天）

> 目标：实现麦克风音频捕获、频谱分析、波形渲染，以及语音识别。

---

### T4.1 — 麦克风捕获 + 频谱分析

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | P1 完成 |
| **描述** | 使用 Web Audio API 捕获麦克风并进行实时频谱分析 |

**子任务：**

- [ ] 创建 `src/hooks/useAudioCapture.ts`
- [ ] `navigator.mediaDevices.getUserMedia({ audio: true })` 获取麦克风权限
- [ ] 创建 `AudioContext` + `AnalyserNode`（fftSize=256）
- [ ] 用 `requestAnimationFrame` 定期读取 `getByteFrequencyData()`
- [ ] 计算 RMS 音量（`getByteTimeDomainData()` 求 RMS）
- [ ] 将数据写入 Zustand store：`audioLevel`, `frequencyData`
- [ ] 组件卸载时释放 MediaStream 资源

**验收标准：**
- 能获取麦克风权限
- 实时看到 `frequencyData` 和 `audioLevel` 变化
- 无内存泄漏

---

### T4.2 — 胶囊内波形条（Canvas 2D）

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T4.1 |
| **描述** | 在 Dynamic Island 胶囊内绘制彩色柱状频谱 |

**子任务：**

- [ ] 创建 `src/components/Waveform/WaveformBars.tsx`
- [ ] Canvas 2D 绘制 4-5 根竖条
- [ ] 竖条高度从 0 到 `frequencyData` 映射
- [ ] 渐变色填充（蓝 → 紫 → 粉）
- [ ] 圆角矩形 `roundRect()`
- [ ] 峰值回落动画（peak hold + decay）
- [ ] 禁止 Canvas 上的鼠标事件穿透

**验收标准：**
- 说话时波形条高度变化
- 颜色渐变美观
- 峰值有回落动画
- 60 FPS 流畅

---

### T4.3 — 环形粒子波形（Three.js，可选）

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 4h |
| **依赖** | T4.1, T2.1 |
| **描述** | 在气泡周围使用 Three.js 粒子实现环形频谱（高级效果） |

**子任务：**

- [ ] 创建 `src/components/Waveform/WaveformRing.tsx`
- [ ] 使用 `THREE.Points` / `InstancedMesh`，64 个粒子分布椭圆环
- [ ] 粒子半径 = 基础半径 + `frequencyData[i]` × 振幅
- [ ] 粒子颜色从蓝色渐变到紫色（HSL 色相偏移）
- [ ] 粒子大小随音量微变
- [ ] 聆听模式下显示环形波形，非聆听模式隐藏

**验收标准：**
- 聆听时气泡周围有环形粒子
- 粒子随声音跳动
- 视觉效果震撼
- 性能可接受（≥ 50 FPS）

---

### T4.4 — 语音识别（Web Speech API）

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T4.1 |
| **描述** | 集成 Web Speech API 进行实时语音转文字 |

**子任务：**

- [ ] 创建 `src/services/speechRecognition.ts`
- [ ] 初始化 `SpeechRecognition`，配置 `lang: 'zh-CN'`, `continuous: true`, `interimResults: true`
- [ ] 实时中间结果：边说话边显示识别文字
- [ ] 最终结果：静音 1.5 秒后自动提交
- [ ] 错误处理（无网络、权限拒绝）
- [ ] 与 Zustand store 集成

**验收标准：**
- 说中文能被正确识别为文字
- 中间结果实时更新
- 静音后自动发送
- 权限被拒时有友好提示

---

### T4.5 — TTS 语音合成（可选）

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 3h |
| **依赖** | P5 完成后 |
| **描述** | 将 AI 回复通过 Edge TTS 朗读出来 |

**子任务：**

- [ ] 使用 Web Speech API `SpeechSynthesis` 或 Edge TTS API
- [ ] 配置中文语音（`zh-CN-XiaoxiaoNeural` 女声）
- [ ] 仅在用户设置开启 TTS 时朗读
- [ ] 流式场景：等待完整句子再朗读（非逐字）
- [ ] 朗读时波形切换为回答模式波形

**验收标准：**
- AI 回复能通过语音朗读
- 朗读流畅自然
- 有开关控制

---

### T4.6 — 音频生命周期管理

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T4.1 |
| **描述** | 管理音频资源的申请与释放，避免资源泄漏 |

**子任务：**

- [ ] 窗口隐藏时释放麦克风资源
- [ ] 窗口显示时按需重新申请
- [ ] `AudioContext` 的 `suspend()` / `resume()` 管理
- [ ] 监听 `AudioContext.state` 变化
- [ ] 确保多次切换不产生多个 MediaStream

**验收标准：**
- 窗口隐藏后麦克风指示灯熄灭（系统层面）
- 资源正确释放，无泄漏
- 100 次显示/隐藏循环后内存稳定

---

### T4.7 — 权限引导 UI

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 1.5h |
| **依赖** | T4.1 |
| **描述** | 当麦克风权限被拒绝时，显示友好引导 |

**子任务：**

- [ ] 检测 `NotAllowedError` 异常
- [ ] 显示提示文字："需要麦克风权限才能使用语音功能"
- [ ] 提供"打开设置"按钮（引导到 Windows 隐私设置）
- [ ] 无麦克风时切换到纯文字模式

**验收标准：**
- 权限拒绝时有明确提示
- 用户知道如何开启权限
- 不影响文字输入功能

---

### T4.8 — 模拟音频数据（开发用）

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 1h |
| **依赖** | T4.1 |
| **描述** | 无麦克风时提供模拟波形数据，方便开发调试 |

**子任务：**

- [ ] 创建 `mockFrequencyData()` 函数
- [ ] 生成正弦波 + 随机噪声的模拟频谱
- [ ] 通过环境变量 `VITE_MOCK_AUDIO=true` 切换
- [ ] 在开发时默认启用（方便测试波形 UI）

**验收标准：**
- 开发环境下无需麦克风也能看到波形效果
- 生产环境不使用模拟数据

---

## P5：ChatGPT 集成 + 对话面板（3-4 天）

> 目标：接入 OpenAI API，实现流式对话、对话面板 UI、历史管理。

---

### T5.1 — OpenAI API 封装

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | P1 完成 |
| **描述** | 封装 OpenAI Chat Completion SSE 流式调用 |

**子任务：**

- [ ] 创建 `src/services/openai.ts`
- [ ] 实现 `streamChat()` 函数（SSE 流式解析）
- [ ] 支持自定义 `baseURL`（代理）
- [ ] 支持 `gpt-4o` / `gpt-4o-mini` 模型切换
- [ ] 错误分类处理（401/429/5xx/网络错误）
- [ ] 指数退避重试（1s → 2s → 4s，最多 3 次）

**验收标准：**
- 能够调用 OpenAI API 并获取流式响应
- 错误处理覆盖主要错误类型
- API Key 可配置

---

### T5.2 — 流式响应处理 + 打字机效果

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | T5.1 |
| **描述** | 处理 SSE token 流，实现逐字输出的打字机效果 |

**子任务：**

- [ ] 创建 `src/components/ChatPanel/StreamingText.tsx`
- [ ] 接收 token 流，逐 token append 到消息内容
- [ ] 自动滚动到底部（跟随最新内容）
- [ ] 处理特殊 token：空内容、换行、代码块标记
- [ ] `isStreaming` 状态管理

**验收标准：**
- AI 回复逐字输出，像打字机一样
- 自动滚动跟随最新内容
- Markdown 在流式输出时不会渲染错误（等完整块再渲染）

---

### T5.3 — 对话面板 UI

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 5h |
| **依赖** | T3.3 |
| **描述** | 实现完整的对话面板 UI |

**子任务：**

- [ ] 创建 `src/components/ChatPanel/index.tsx` 面板容器
- [ ] 创建 `src/components/ChatPanel/MessageBubble.tsx` 消息气泡
- [ ] 用户消息：右对齐，蓝色/紫色气泡
- [ ] AI 消息：左对齐，灰色/透明气泡
- [ ] 消息列表虚拟滚动（`react-window` 或原生滚动）
- [ ] 底部输入栏：语音按钮 + 文字输入 + 发送按钮
- [ ] 输入框自动聚焦
- [ ] Enter 发送，Shift+Enter 换行
- [ ] 消息时间戳显示

**验收标准：**
- 面板 UI 美观，布局合理
- 消息气泡样式好
- 输入体验流畅

---

### T5.4 — Markdown 渲染

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 3h |
| **依赖** | T5.3 |
| **描述** | AI 回复支持 Markdown 渲染和代码块语法高亮 |

**子任务：**

- [ ] 安装 `react-markdown`, `react-syntax-highlighter`
- [ ] AI 消息中使用 `ReactMarkdown` 组件
- [ ] 代码块使用 `react-syntax-highlighter` 高亮
- [ ] 代码块添加复制按钮
- [ ] 支持表格、列表、引用等 Markdown 语法
- [ ] 暗色主题代码块样式

**验收标准：**
- Markdown 格式正确渲染
- 代码块有语法高亮
- 代码块可以一键复制

---

### T5.5 — 对话历史管理

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | T5.2 |
| **描述** | 管理多轮对话上下文和历史记录 |

**子任务：**

- [ ] 创建 `src/store/chatStore.ts`（Zustand + persist）
- [ ] 定义 `Conversation` 类型：`id, title, messages[], createdAt`
- [ ] 支持新建对话
- [ ] 支持切换历史对话
- [ ] 支持删除对话
- [ ] `localStorage` 持久化（Zustand persist middleware）
- [ ] 对话标题自动生成（取前 30 个字符或 AI 总结）

**验收标准：**
- 多轮对话上下文正确传递
- 刷新后对话不丢失
- 可以管理多个对话

---

### T5.6 — 上下文裁剪

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T5.5 |
| **描述** | 超长对话自动裁剪上下文，防止 token 超限 |

**子任务：**

- [ ] 实现 `trimContext()` 函数
- [ ] 保留 system prompt + 最近 N 轮对话
- [ ] 基于 token 估算裁剪（1 char ≈ 0.25 token）
- [ ] 裁剪边界在消息边界（不截断单条消息）
- [ ] 裁剪时给 AI 插入提示"早期对话已省略"

**验收标准：**
- 超长对话不会报 token 超限错误
- 裁剪后的上下文仍保持连贯性
- 裁剪逻辑不影响性能

---

### T5.7 — 错误与空状态处理

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T5.3 |
| **描述** | 完善各种边界情况的 UI 处理 |

**子任务：**

- [ ] 无对话时的空状态提示（"有什么可以帮你的？"）
- [ ] API Error 时的错误提示（网络 / 认证 / 服务端）
- [ ] 重试按钮（重新发送最后一条消息）
- [ ] Loading 骨架屏
- [ ] 空消息不允许发送

**验收标准：**
- 各种边界情况有对应的 UI 反馈
- 错误提示清晰易懂
- 用户可以便捷重试

---

### T5.8 — API Key 加密存储

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 2h |
| **依赖** | T5.1 |
| **描述** | 使用 Electron safeStorage 加密存储 API Key |

**子任务：**

- [ ] 主进程中实现 `encrypt-api-key` / `decrypt-api-key` IPC handler
- [ ] 使用 `safeStorage.encryptString()` / `decryptString()`
- [ ] preload 暴露加密解密 API
- [ ] 渲染进程不直接接触明文 API Key
- [ ] 设置页面的 API Key 输入框使用 `type="password"`

**验收标准：**
- API Key 不在渲染进程内存中明文存储
- 加密后的 Key 持久化到本地
- Windows 非管理员用户无法直接读取

---

### T5.9 — 新对话引导

| 属性 | 内容 |
|------|------|
| **优先级** | P2 |
| **预计工时** | 1.5h |
| **依赖** | T5.3 |
| **描述** | 首次使用或新对话时的引导 UI |

**子任务：**

- [ ] 首次使用显示欢迎页面
- [ ] 提供几个预设问题建议（quick prompts）
- [ ] "试试问我：今天的天气 / 帮我写一封邮件 / ..."
- [ ] 点击预设问题自动发送

**验收标准：**
- 新对话有友好的欢迎引导
- 预设问题可点击使用
- 不影响正常输入

---

## P6：设置 + 打包 + 发布（3-5 天）

> 目标：完善设置面板、系统集成、打包发布、最终打磨。

---

### T6.1 — 设置面板 UI

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 4h |
| **依赖** | P5 基本完成 |
| **描述** | 创建设置面板，管理所有用户配置 |

**子任务：**

- [ ] 创建 `src/components/Settings/index.tsx`
- [ ] API Key 配置（带加密存储）
- [ ] 模型选择（gpt-4o / gpt-4o-mini）
- [ ] 自定义 API 代理地址
- [ ] 快捷键自定义（默认 Alt+Space）
- [ ] 开机自启开关
- [ ] TTS 语音开关
- [ ] 外观模式（跟随系统 / 亮色 / 暗色）
- [ ] 语言设置
- [ ] 设置入口：托盘菜单 → 设置，或面板内设置图标

**验收标准：**
- 所有设置项可配置
- 设置实时生效（部分需重启）
- 界面整洁美观

---

### T6.2 — 开机自启

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 1.5h |
| **依赖** | T6.1 |
| **描述** | 支持开机自启和手动开关**

**子任务：**

- [ ] 使用 `app.setLoginItemSettings({ openAtLogin: true })`
- [ ] 与设置面板的开机自启开关联动
- [ ] 安装时询问是否开启

**验收标准：**
- 开启后重启电脑自动启动
- 关闭后不自动启动
- 开关状态正确保存

---

### T6.3 — 暗色模式检测与适配

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T6.1 |
| **描述** | 跟随系统暗色/亮色模式切换外观 |

**子任务：**

- [ ] Windows 暗色模式检测（`nativeTheme.shouldUseDarkColors`）
- [ ] 通过 IPC 传递给渲染进程
- [ ] 监听 `nativeTheme.updated` 事件实时切换
- [ ] 气泡玻璃参数在暗色/亮色下差异调整
- [ ] 面板和文字配色自适应

**验收标准：**
- 切换系统暗色模式后，气泡和面板跟随变化
- 暗色模式下玻璃效果依然美观
- 亮色/暗色下文字可读性都好

---

### T6.4 — 应用图标设计

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | — |
| **描述** | 设计和准备应用图标（多尺寸） |

**子任务：**

- [ ] 设计应用主图标（512×512 PNG）
- [ ] 生成 .ico 文件（16/32/48/256 尺寸）
- [ ] 托盘图标（16×16 和 32×32）
- [ ] 安装包图标
- [ ] 图标设计方向：液态玻璃气泡的简约风格

**验收标准：**
- 图标在各场景下清晰可辨
- 风格与应用视觉一致

---

### T6.5 — 错误日志与崩溃报告

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 3h |
| **依赖** | P5 基本完成 |
| **描述** | 添加错误日志和崩溃捕获机制**

**子任务：**

- [ ] 使用 `electron-log` 记录主进程日志
- [ ] 渲染进程错误通过 IPC 发送到主进程记录
- [ ] 未捕获异常全局处理（`process.on('uncaughtException')`）
- [ ] 崩溃时优雅关闭，记录 dump
- [ ] 日志轮转（7 天清理）
- [ ] 日志目录：`%APPDATA%/SiriAI/logs/`

**验收标准：**
- 所有错误有日志记录
- 崩溃后可以查看日志
- 日志不会无限增长

---

### T6.6 — 性能优化

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 4h |
| **依赖** | P5 完成 |
| **描述** | 全面性能优化，确保流畅体验**

**子任务：**

- [ ] React 组件 memo 优化（`React.memo`, `useMemo`, `useCallback`）
- [ ] Zustand selector 精准订阅（避免不必要的重渲染）
- [ ] Three.js `useFrame` 节流（仅在需要时更新 uniforms）
- [ ] 窗口不可见时暂停 rAF 循环
- [ ] 图片/字体懒加载
- [ ] Vite 生产构建 tree shaking + code splitting
- [ ] 目标：空闲 CPU < 2%，内存 < 200MB

**验收标准：**
- 性能指标达到 PRD 要求
- Chrome DevTools Performance 无长任务（>50ms）
- 低端设备（i5 集显）也能流畅运行

---

### T6.7 — electron-builder 打包配置

| 属性 | 内容 |
|------|------|
| **优先级** | P0 |
| **预计工时** | 3h |
| **依赖** | P5 完成 |
| **描述** | 配置打包流水线，生成可分发安装包**

**子任务：**

- [ ] 完善 `electron-builder.yml` 配置
- [ ] NSIS 安装包配置（安装路径选择、桌面快捷方式、开始菜单）
- [ ] MSIX 应用包配置（可选）
- [ ] 代码签名（如有证书）
- [ ] 自动更新配置（`electron-updater`）
- [ ] 打包脚本：`pnpm package`（构建 + 打包）
- [ ] 测试安装/卸载流程

**验收标准：**
- 生成可安装的 .exe 安装包
- 安装流程无错误
- 卸载后无残留
- 安装包大小 < 150 MB

---

### T6.8 — README + 发布准备

| 属性 | 内容 |
|------|------|
| **优先级** | P1 |
| **预计工时** | 2h |
| **依赖** | T6.7 |
| **描述** | 编写 README 文档和发布材料**

**子任务：**

- [ ] 编写 `README.md`：项目介绍、功能特性、安装指南、开发指南
- [ ] 准备截图/GIF 展示
- [ ] 编写 `CHANGELOG.md`
- [ ] 补充 `CONTRIBUTING.md`（如需开源）
- [ ] GitHub Release 页面准备

**验收标准：**
- README 清晰完整
- 新开发者能按文档跑起项目
- 用户能按文档安装使用

---

## 附录

### A. 环境准备 Checklist

- [ ] Node.js ≥ 20 LTS
- [ ] pnpm ≥ 9
- [ ] Visual Studio Build Tools（Windows native 模块编译）
- [ ] Git
- [ ] Windows 11 开发机（支持透明窗口）
- [ ] 麦克风设备（语音功能开发）
- [ ] OpenAI API Key（对话功能开发）

### B. 开发命令速查

| 命令 | 说明 |
|------|------|
| `pnpm dev` | 启动开发环境（Electron + Vite HMR） |
| `pnpm build` | 构建生产版本 |
| `pnpm package` | 构建 + 打包为安装包 |
| `pnpm lint` | ESLint 代码检查 |
| `pnpm typecheck` | TypeScript 类型检查 |
| `pnpm test` | 运行单元测试 |
| `pnpm test:e2e` | 运行端到端测试 |

### C. 关键文件索引

| 文件 | 用途 |
|------|------|
| `electron/main.ts` | Electron 主进程入口 |
| `electron/windowManager.ts` | 透明窗口管理 |
| `electron/tray.ts` | 系统托盘 |
| `electron/preload.ts` | 安全桥接层 |
| `src/App.tsx` | React 根组件 |
| `src/components/LiquidBubble/` | 液态玻璃气泡 |
| `src/components/DynamicIsland/` | 灵动岛展开面板 |
| `src/components/Waveform/` | 波形动画 |
| `src/components/ChatPanel/` | 对话面板 |
| `src/store/appStore.ts` | 全局状态管理 |
| `src/store/chatStore.ts` | 对话状态管理 |
| `src/services/openai.ts` | OpenAI API 封装 |
| `src/services/speechRecognition.ts` | 语音识别服务 |
